# Audit sheet — SC HB 3796

**Record id:** `sc-hb3796-2025`  
**Auditor:** _______________  **Date:** __________

Read the sources below and fill in every `answer.` line. Write `NOT STATED` if the source does not say, `UNREACHABLE` if you cannot retrieve it, `CANNOT TELL` where a classification or a provision test cannot be applied to what you can read. Do not guess, do not infer from context beyond the one classification the conventions permit, and do not consult `registry/bills.json` — the comparison happens afterwards, mechanically.

## Sources

- https://www.scstatehouse.gov/billsearch.php?billnumbers=3796&session=126&summary=B
- https://legiscan.com/SC/bill/H3796/2025

If you use a source that is not listed, add its URL here.

## Conventions

**Which source counts.** Prefer the legislature's own page, the official bill text, or the enrolled or enacted act. Use a tracker only where the official source is unavailable or silent on the field. Where sources conflict, answer from the official one and describe the conflict in `answer.notes`. Name any source you used that is not listed above.

**`status_stage` is the one field you may classify.** It converts a source history into this registry's controlled vocabulary, so reading the history and choosing a category is expected here — and nowhere else. Two cautions. Choose from the latest material action shown: a committee referral with nothing after it is `in_committee`; one chamber's passage with the other silent is `passed_one_chamber`; an approval, signature or enacted act is `enacted`; an expressly recorded defeat, rejection, withdrawal, or veto without override is `failed`. And **this project has not documented what separates `failed` from `dead`** — so if the only ground for a terminal status is that the session ended, write `CANNOT TELL` and say so in `answer.notes` rather than picking one. Same answer for any history you cannot classify without an added assumption.

**Do not compute a date the source does not print.** Many states have a default effective date; deriving one is exactly the kind of inference this audit exists to detect.

## Fields

**bill_number** — The bill number exactly as the source prints it (e.g. 'H. 3796', 'HB 1462').

```
answer.bill_number: 
```

**chamber** — Which chamber introduced it? house / senate / joint.

```
answer.chamber: 
```

**year_introduced** — Calendar year of introduction. The year in an explicitly dated introduction action counts as stated.

```
answer.year_introduced: 
```

**session** — The session as the source names it (e.g. '126th General Assembly, 1st Reg. Sess.').

```
answer.session: 
```

**status_stage** — Current disposition, one of: introduced · in_committee · passed_one_chamber · enacted · failed · dead. Classify from the history per the conventions above, or write CANNOT TELL.

```
answer.status_stage: 
```

**status_action** — The MOST RECENT action line, copied verbatim from the official legislative history. Do not substitute a tracker's prose summary where an official history exists.

```
answer.status_action: 
```

**status_action_date** — Date of that action, as printed.

```
answer.status_action_date: 
```

**sponsors** — Every person named in the authoritative page's sponsor, author, co-sponsor or co-author fields, separated by semicolons and spelled as printed. Do not reconstruct sponsorship from history entries. If the page distinguishes primary from additional sponsors, record that distinction in `answer.notes`.

```
answer.sponsors: 
```

**codified_at** — Which code sections the bill or act expressly creates, amends or repeals. Do not infer codification from a subject heading or a tracker summary.

```
answer.codified_at: 
```

**effective_date** — Effective date only where the bill text, the act, or official legislative metadata expressly states one — do not calculate a default. If text and metadata disagree, record the text's date and note the conflict. If none is stated, write NOT STATED.

```
answer.effective_date: 
```

**operative_quote** — The sentence that denies personhood, declares AI non-sentient, or otherwise sets AI's legal status — copied VERBATIM. Where distinct legal-status rules need more than one sentence, quote the shortest contiguous passage containing them and explain the choice in `answer.notes`; never paraphrase. If the source you can reach does not contain operative text, write NOT STATED; if the operative text exists but you cannot retrieve it, write UNREACHABLE.

```
answer.operative_quote: 
```

## Provision tests

Apply each test to the operative text. Answer `yes`, `no`, or `CANNOT TELL` if you could not read the operative text. These are textual tests, not judgements about legal effect.

**denies_legal_personhood**
> The text states that an AI system may not be granted, or may not be recognised as having, legal personhood or the status of a person.

```
answer.provision.denies_legal_personhood: 
```

**declares_non_sentient**
> The text asserts as a matter of law that AI does not possess sentience, consciousness, self-awareness or similar traits.

```
answer.provision.declares_non_sentient: 
```

**assigns_liability_to_humans**
> The text affirmatively allocates liability for AI-caused harm to a human or corporate actor.

```
answer.provision.assigns_liability_to_humans: 
```

**bars_ai_liability**
> The text states that liability may not be imposed on the AI system itself.

```
answer.provision.bars_ai_liability: 
```

**covers_non_ai_entities**
> The text extends the same prohibition to non-AI entities such as animals, bodies of water or inanimate objects.

```
answer.provision.covers_non_ai_entities: 
```

## Notes

Anything the sheet did not ask for: a source that contradicts another, a source you used that was not listed, a sponsorship-role distinction, why you answered CANNOT TELL, an operative provision that spans several sentences, or a field you think is unanswerable from any public document.

```
answer.notes: 
```
